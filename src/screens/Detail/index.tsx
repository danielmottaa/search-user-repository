import { useEffect } from "react";
import { useSelector } from "react-redux";

const Detail = () => {
  // const { activeUser }: any = useSelector();

  // useEffect(() => {
  //   console.log(activeUser)
  // }, [])

    return (
      <div>Hello detail</div>  
    );
  }
   
  export default Detail;